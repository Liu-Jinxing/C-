#include<stdio.h>
int main()
{
    int i,a,b,count;

    for(i=2;i<=200;i++)
    {
        a=i;
        b=i;
        for(count=1;b!=1;b--)
        {
            if((a%b)==0)
                count++;
        }
        if(count<=2)
            printf("%d\n",i);
    }
    return 0;
}
